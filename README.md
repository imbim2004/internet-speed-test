# speedtest.py — замер скорости интернета

Скрипт замеряет скорость скачивания с вашего компьютера: делает N **последовательных**
GET-запросов к указанному адресу (например, к тяжёлой картинке), дожидается полного ответа
каждого, и печатает среднее время запроса, объём скачанных данных и скорость в МБ/с.

Зависимостей нет — только стандартная библиотека Python 3 (требуется Python 3.8+).

## Установка

```bash
git clone https://github.com/imbim2004/internet-speed-test.git
cd internet-speed-test
chmod +x speedtest.py
```

## Использование

```bash
python3 speedtest.py <URL>
```

Пример (тяжёлая картинка ~9.7 МБ):

```bash
python3 speedtest.py "https://upload.wikimedia.org/wikipedia/commons/f/ff/Pizigani_1367_Chart_10MB.jpg"
```

### Ключи

| Ключ | Значение | По умолчанию |
|---|---|---|
| `URL` | адрес, куда стучаться (обязательный аргумент) | — |
| `-n`, `--requests` | количество последовательных запросов | `10` |
| `-t`, `--timeout` | таймаут одного запроса, секунды | `60` |
| `--warmup` | сделать прогревочный запрос, не учитываемый в статистике | выкл. |
| `--no-cache-buster` | не добавлять уникальный query-параметр к URL | выкл. |
| `-q`, `--quiet` | не печатать строку по каждому запросу, только итог | выкл. |

## Пример вывода

```
URL:      https://upload.wikimedia.org/wikipedia/commons/f/ff/Pizigani_1367_Chart_10MB.jpg
Запросов: 10

[ 1/10] HTTP 200    5.110 с     9.70 МБ     1.90 МБ/с
[ 2/10] HTTP 200    4.258 с     9.70 МБ     2.28 МБ/с
...
[10/10] HTTP 200    4.019 с     9.70 МБ     2.41 МБ/с

====================================================
Успешных запросов:     10 из 10
Среднее время запроса: 4.472 с
  мин / макс:          4.019 с / 5.110 с
  медиана:             4.401 с
Средний объём ответа:  9.70 МБ
Всего скачано:         97.03 МБ (101747060 байт)
Общее время:           44.720 с
----------------------------------------------------
СКОРОСТЬ:              2.17 МБ/с  (18.21 Мбит/с)
====================================================
```

## Как считается скорость

- Время запроса меряется от отправки запроса до чтения последнего байта тела ответа
  (`time.perf_counter()`), то есть включает DNS, TCP/TLS-хендшейк, TTFB и саму загрузку.
- Тело ответа читается потоком по 64 КБ и не держится в памяти целиком — считаются только байты.
- **Скорость** = общий объём скачанного / суммарное время всех успешных запросов.
  Выводится и в МБ/с (мегабайты, 1 МБ = 1024²  байт), и в Мбит/с (мегабиты, как у провайдеров).
- К каждому URL добавляется уникальный параметр `?_cb=<timestamp>-<номер>` и заголовки
  `Cache-Control: no-cache`, чтобы ответ не пришёл из кэша и замер был честным.
  Отключается ключом `--no-cache-buster`.
- Упавшие запросы (таймаут, DNS, HTTP-ошибка) не ломают прогон: они логируются в stderr,
  а статистика считается по успешным. Если не удался ни один — код возврата `1`.

## Что выбрать в качестве URL

Файл должен быть достаточно большим (от ~5 МБ), иначе результат будет мерить в основном
задержку соединения, а не пропускную способность. Также отдача должна идти без сжатия —
скрипт не запрашивает `gzip`, но у уже сжатых форматов (JPEG, PNG, `.bin`) этой проблемы нет
в принципе.

## Замечания

- Первый запрос обычно медленнее остальных: холодный DNS, TLS-хендшейк, прогрев соединения
  и кэша CDN. Ключ `--warmup` делает отдельный прогревочный запрос, чтобы это не смазывало статистику.
- Замер однопоточный и последовательный — по ТЗ. Это показывает скорость **одного** соединения,
  которая обычно ниже пиковой скорости канала: многие сервера ограничивают отдачу на соединение.
